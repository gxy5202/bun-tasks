{
  "name": "bun-tasks",
  "version": "0.1.2",
  "description": "A parallel task runner for the Bun runtime inspired by concurrently",
  "type": "module",
  "bin": {
    "bun-tasks": "./dist/bin.js"
  },
  "exports": {
    ".": {
      "import": "./dist/cli.js",
      "types": "./dist/cli.d.ts"
    }
  },
  "main": "dist/cli.js",
  "types": "dist/cli.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
  "build": "rimraf dist && tsgo -p tsconfig.json && rimraf dist/bin.d.ts",
    "test": "bun test --coverage"
  },
  "keywords": [
    "bun",
    "parallel",
    "tasks",
    "runner"
  ],
  "author": "Gomi",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/gxy5202/bun-tasks"
  },
  "bugs": {
    "url": "https://github.com/gxy5202/bun-tasks/issues"
  },
  "dependencies": {
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/chalk": "^2.2.4",
    "@types/node": "^20.12.12",
    "@typescript/native-preview": "^7.0.0-dev.20251011.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
